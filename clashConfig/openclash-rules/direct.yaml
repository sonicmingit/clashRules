# ============================================================
# OpenClash自定义直连规则
# 更新时间：2026-09-17
# behavior: classical
# ============================================================
payload:
  # ----------------------------------------------------------
  # 自建服务
  # ----------------------------------------------------------
  - DOMAIN-SUFFIX,sonicming.top
  # ----------------------------------------------------------
  # Logitech
  # ----------------------------------------------------------
  - DOMAIN,updates.optionsplus.logitechg.com
  # ----------------------------------------------------------
  # VM 网段 / 域名
  # ----------------------------------------------------------
  - IP-CIDR,10.0.10.0/24,no-resolve
  - DOMAIN-KEYWORD,.vm
  # ----------------------------------------------------------
  # 内网地址
  # 10.0.0.0/23 = 10.0.0.0 ~ 10.0.1.255
  # ----------------------------------------------------------
  - IP-CIDR,10.0.0.0/23,no-resolve
  # ----------------------------------------------------------
  # 内网设备直连
  # ----------------------------------------------------------
  - SRC-IP-CIDR,192.168.1.0/24
  # ----------------------------------------------------------
  # uTools
  # ----------------------------------------------------------
  - DOMAIN-SUFFIX,u-tools.cn
  # ----------------------------------------------------------
  # NTP 时间同步
  # ----------------------------------------------------------
  - DOMAIN-SUFFIX,ntp.org
  # ----------------------------------------------------------
  # 其他直连网站
  # ----------------------------------------------------------
  - DOMAIN,gitee.com

  # ----------------------------------------------------------
  # 后续新增
  # ----------------------------------------------------------
  # - DOMAIN-SUFFIX,example.com